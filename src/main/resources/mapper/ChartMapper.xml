<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yupi.springbootinit.mapper.ChartMapper">

    <resultMap id="BaseResultMap" type="com.yupi.springbootinit.model.entity.Chart">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="goal" column="goal" jdbcType="VARCHAR"/>
        <result property="chartData" column="chartData" jdbcType="VARCHAR"/>
        <result property="chartType" column="chartType" jdbcType="VARCHAR"/>
        <result property="genChart" column="genChart" jdbcType="VARCHAR"/>
        <result property="getResult" column="getResult" jdbcType="VARCHAR"/>
        <result property="chartStatus" column="chartStatus" jdbcType="VARCHAR"/>
        <result property="execMessage" column="execMessage" jdbcType="VARCHAR"/>
        <result property="userId" column="userId" jdbcType="BIGINT"/>
        <result property="createTime" column="createTime" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="updateTime" jdbcType="TIMESTAMP"/>
        <result property="isDelete" column="isDelete" jdbcType="TINYINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        id
        ,`name`
        ,goal,chartData,
        chartType,genChart,getResult,
        chartStatus,execMessage,
        userId,createTime,updateTime,
        isDelete
    </sql>

<!--    查询具体图表-->
    <select id="queryChartData" resultType="java.util.Map">${queryChartSql}</select>

<!--    分页查询优化-->
    <select id="selectByPage" resultType="com.yupi.springbootinit.model.entity.Chart">
        SELECT * FROM chart a inner join (SELECT id
                                          FROM chart
                                          WHERE userId = #{userId} and isDelete = 0
                                          order by createTime DESC
                                          LIMIT #{offset} , #{size}
        ) b on a.id=b.id;
    </select>
    <select id="selectTotal" resultType="long">
        SELECT COUNT(*) AS total FROM chart WHERE isDelete = 0 AND userId = #{userId};
    </select>
</mapper>
